# Optional. The app works fully without these — grievance triage and blocker
# rewriting fall back to a deterministic on-device classifier and canned copy.
# Set ONE of these to enable the model path.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5

ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-opus-5
